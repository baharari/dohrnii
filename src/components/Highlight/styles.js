import { StyleSheet } from 'react-native';

const createStyles = (theme) => {
  return StyleSheet.create({
    container: {
      // Dış Text stiline kullanıcıdan gelen style eklenir
    },
  });
};

export default createStyles;
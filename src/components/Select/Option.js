import React from 'react';
import PropTypes from 'prop-types';
import { SelectOptionPropTypes, SelectOptionDefaultProps } from './types';

const Option = () => null;

Option.propTypes = SelectOptionPropTypes;
Option.defaultProps = SelectOptionDefaultProps;
Option.displayName = 'SelectOption';

export default Option;